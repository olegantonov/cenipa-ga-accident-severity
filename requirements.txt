# Python 3.12. Pinned to the versions used to produce the results.
# Install:  python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#           python -m camoufox fetch   # baixa o navegador anti-detecção (scraping)

# --- Scraping (contorna o Cloudflare Turnstile do CENIPA) ---
camoufox[geoip]==0.4.11

# --- Extração de PDF ---
PyMuPDF==1.27.2.3

# --- Dados e análise ---
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.8.0
matplotlib==3.10.9

# --- Geração do manuscrito (.docx) ---
python-docx==1.2.0
